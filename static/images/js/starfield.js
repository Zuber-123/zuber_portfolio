const canvas = document.getElementById('starfield'); const ctx = canvas.getContext('2d'); let stars = []; function resize() { canvas.width = window.innerWidth; canvas.height = window.innerHeight; generate() } function generate() { const count = Math.floor(canvas.width * canvas.height / 8000); stars = new Array(count).fill(0).map(() => ({ x: Math.random() * canvas.width, y: Math.random() * canvas.height, r: Math.random() * 1.6 + 0.2, s: Math.random() * 0.4 + 0.1 })) } function step() { ctx.clearRect(0, 0, canvas.width, canvas.height); ctx.fillStyle = '#ffffff'; for (const st of stars) { ctx.globalAlpha = 0.7; ctx.beginPath(); ctx.arc(st.x, st.y, st.r, 0, Math.PI * 2); ctx.fill(); st.y += st.s; if (st.y > canvas.height) { st.y = -2; st.x = Math.random() * canvas.width } } requestAnimationFrame(step) } window.addEventListener('resize', resize); resize(); step();
